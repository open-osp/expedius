# This is a sample build configuration for Java (Maven).
# Check our guides at https://confluence.atlassian.com/x/zd-5Mw for more examples.
# Only use spaces to indent your .yml configuration.
# -----
# You can specify a custom docker image from Docker Hub as your build environment.
image: maven:3-jdk-8

pipelines:
  default:
    - step:
        caches:
          - maven
        script: # Modify the commands below to build your repository.
          - mvn -B -Dmaven.test.skip=true package # -B batch mode makes Maven less verbose
    - step:
        name: Deploy to Release
        deployment: Current-Release
        # trigger: manual  # Uncomment to make this a manual deployment.
        script:
           - pipe: atlassian/bitbucket-upload-file:0.1.3
             variables:
               BITBUCKET_USERNAME: 'dwarren@colcamex.com'
               BITBUCKET_APP_PASSWORD: 'L3mD7TpjgX'
               FILENAME: 'Expedius-4.12.19.war'
               # ACCOUNT: '<string>' # Optional
               # REPOSITORY: '<string>' # Optional
               # DEBUG: '<boolean>' # Optional

